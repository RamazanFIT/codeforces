#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    int size;cin>>size;
    while(size--){
        int massive[4][4];
        for(int i = 0; i < 3; i++){
            for(int j = 0; j < 3; j++){

                cin >> massive[i][j];
            }
        }
    }
    


}

